{
  "name": "@yimengweixing/studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run types && npm run typecheck && npm run test && wrangler deploy --dry-run --outdir ../../.wrangler/studio-dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.9.9"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.38.0"
  }
}
